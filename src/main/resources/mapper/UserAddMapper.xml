<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ysl.calendar.user.repository.mybatis.UserAddMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into cal_user (id, email, name, nickname, password, image, comment, social)
        values (#{id}, #{email}, #{name}, #{nickname}, #{password}, #{image}, #{comment}, #{social})
    </insert>

    <select id="findByEmail" parameterType="String" resultType="com.ysl.calendar.domain.users.CalendarUser">
        select id, email, name, nickname, password, image, comment, social from cal_user where email = #{email}
    </select>
</mapper>