spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: 이메일 주소
    password: 이메일 전송 패스워드
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    auth-code-expiration-millis: 180000  # 3 * 60 * 1000 == 3분